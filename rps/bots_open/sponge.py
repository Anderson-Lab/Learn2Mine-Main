# sponge
if input == "": # initialize variables for the first round
	rockCount = paperCount = scissorsCount = 0
    move = input
lastMove = move
move = input

if lastMove == move:
    output = move
else: 
    output = lastMove